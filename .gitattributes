# Shell scripts run on the Linux VPS — must always check out with LF endings,
# even when cloned or edited on Windows. CRLF here produces:
#   /usr/bin/env: 'bash\r': No such file or directory
*.sh text eol=lf
